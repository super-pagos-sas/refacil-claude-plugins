# Keep vendored skill files byte-identical to the npm-published source.
# Treat them as binary blobs (-text) so git never rewrites their EOL — the
# contract test CA-04 compares these files byte-for-byte against the global
# npm install, whose line endings must be preserved exactly as published.
refacil-pay/skills/refacil-pay-cli/** -text
